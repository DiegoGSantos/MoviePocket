<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="android.view.View"/>

        <variable
            name="viewModel"
            type="com.moviepocket.features.moviesList.viewmodel.MoviesViewModel" />
    </data>

    <com.moviepocket.customViews.TouchableLinearLayout
        android:id="@+id/mainLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center">

            <android.support.v7.widget.RecyclerView
                android:id="@+id/moviesList"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="@dimen/nano_margin"
                android:clipToPadding="false"
                android:visibility="@{!viewModel.isLoading &amp;&amp; !viewModel.hasError ? View.VISIBLE : View.GONE}"
                android:gravity="center" />

            <com.moviepocket.customViews.LoadingView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="@{viewModel.isLoading &amp;&amp; !viewModel.hasError ? View.VISIBLE : View.GONE}"/>

            <com.moviepocket.customViews.ErrorView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:visibility="@{!viewModel.isLoading &amp;&amp; viewModel.hasError ? View.VISIBLE : View.GONE}"/>

        </FrameLayout>

    </com.moviepocket.customViews.TouchableLinearLayout>
</layout>
